/**
 * 
    Stocker les différentes valeurs dans un tableau
    Créer les paires de digit : 
    Lire la ligne
        Lire chaque caractère
        Déterminer si chaque caractère est un nombre ou une lettre
        ne retourner que si c'est un nombre --> Je dois donc stocker à la fois la ligne après parsing, et l'ensemble des lignes parsées
        

    Additionner toutes les valeurs pour obtenir la valeur finale
*/
